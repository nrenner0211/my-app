{
  "name": "nixwebdev",
  "version": "0.1.0",
  "homepage": "https://nixwebdev.com/",
  "private": true,
  "dependencies": {
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "npm run dev",
    "build": "npm build",
    "deploy": "gh-pages -d build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
